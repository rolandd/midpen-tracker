# Midpen-Tracker Local Development Configuration
# Copy this to .env and fill in your values

# --- Build Config ---
TS_RS_EXPORT_DIR=.

# --- Required (from Strava API settings) ---
STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret

# --- Required for auth ---
# Generate with: openssl rand -base64 32
JWT_SIGNING_KEY=your_32_byte_signing_key_here

# --- Required for webhook setup ---
# Can be any random string you choose
WEBHOOK_VERIFY_TOKEN=your_webhook_verify_token

# --- Optional (with defaults) ---
# PORT=8080
# FRONTEND_URL=http://localhost:5173
# GCP_PROJECT_ID=local-dev

# --- For production (Cloud Run) ---
# These are set automatically via Secret Manager bindings
# STRAVA_CLIENT_SECRET, JWT_SIGNING_KEY, WEBHOOK_VERIFY_TOKEN
