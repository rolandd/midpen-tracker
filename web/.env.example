# Local Development Configuration
# Copy this to .env (gitignored) for local overrides

# Enable demo mode to use synthetic data without backend
# PUBLIC_DEMO_MODE=true

# API URL override (default: http://localhost:8080)
# PUBLIC_API_URL=http://localhost:8080

# Base URL for OpenGraph/Twitter meta tags (no trailing slash)
# PUBLIC_BASE_URL=https://midpen-tracker.pages.dev
