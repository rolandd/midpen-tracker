  # 1. Pull the previous builder image to leverage layer caching
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args: ['-c', 'docker pull ${_REGION}-docker.pkg.dev/${PROJECT_ID}/midpen-strava/api:builder || exit 0']

  # 2. Build the builder stage (with dependencies) using cached layers
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--target'
      - 'builder'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/midpen-strava/api:builder'
      - '--cache-from'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/midpen-strava/api:builder'
      - '.'

  # 3. Build the final runtime image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/midpen-strava/api:latest'
      - '--build-arg'
      - 'BUILD_ID=${SHORT_SHA}'
      - '--cache-from'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/midpen-strava/api:builder'
      - '.'

  # 4. Push the images (builder cache + runtime)
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/midpen-strava/api:latest']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/midpen-strava/api:builder']

  # 5. Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'midpen-strava-api'
      - '--image'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/midpen-strava/api:latest'
      - '--region'
      - '${_REGION}'

options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _REGION: us-west1 # Default, overridden by trigger
